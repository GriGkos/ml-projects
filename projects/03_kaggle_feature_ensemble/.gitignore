# Kaggle data can be large and is downloaded locally.
data/*
!data/.gitkeep
!data/README.md

# Keep notebook checkpoints out of the project branch.
notebooks/.ipynb_checkpoints/

# Cached feature matrices and model outputs are rebuilt locally.
artifacts/*
!artifacts/.gitkeep
