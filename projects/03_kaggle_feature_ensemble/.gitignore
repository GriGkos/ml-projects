# Local Kaggle data and all generated caches are never part of the submission.
data/
artifacts/

# The notebook contains the full executable pipeline. These local helpers are
# kept only for development and must not be published with the project.
src/
docs/

# Only the final Kaggle file belongs in the repository.
submissions/candidates/
submissions/submission_v*.csv

# Notebook/Jupyter runtime files.
notebooks/.ipynb_checkpoints/
**/__pycache__/
*.pyc
tmp_*.py
