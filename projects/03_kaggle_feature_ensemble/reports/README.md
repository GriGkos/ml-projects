# Kaggle Evidence

Place the Kaggle submission screenshot here as `kaggle_submission.png` before submitting the project.

The screenshot should visibly show the account username and the public score. It is an external
competition artifact, so it is not generated by the local notebook. The locally generated file
to upload is `../submissions/submission_final.csv`.
